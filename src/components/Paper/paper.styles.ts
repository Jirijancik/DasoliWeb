import styled from 'styled-components';

export const PaperStyled = styled.section`
  background: var(--WHITE_PAPER_BACKGROUND);
  padding: 5% 5%;
  color: var(--WHITE);
  filter: drop-shadow(6px 0px 12px rgba(var(--base_black_code), 0.45));
`
